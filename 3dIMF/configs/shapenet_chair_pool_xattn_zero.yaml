experiment_name: shapenet_chair_pool_xattn_zero
seed: 42
output_dir: runs/shapenet_chair_pool_xattn_zero

data:
  root: data/shapenet_pyg
  category: Chair
  num_workers: 4

model:
  arch: pool_xattn
  num_points: 1024
  hidden_dim: 128
  num_layers: 4
  early_layers: 2
  late_layers: 2
  num_slots: 8
  code_mode: zero
  cross_attn: first
  num_heads: 4
  dropout: 0.0

training:
  batch_size: 16
  lr: 0.0002
  epochs: 5
  log_every: 10
  sample_every: 200
  checkpoint_every: 1000
  device: auto

loss:
  mu: -0.4
  sigma: 1.0
  neq_ratio: 0.5

sampling:
  steps: 50
  num_samples: 4

wandb:
  enabled: false
  project: 3dIMF
